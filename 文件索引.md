# 📚 Qwen3.5 训练工具完整索引

> 基于 `run_train.sh` 创建的可配置训练方案  
> 支持灵活调整 GPU 数量和并行策略，实时监控精度、显存、性能

---

## 🎯 核心问题快速解答

### Q1: 如何调整 GPU 数量？
```bash
# 使用 NGPU 参数
NGPU=1  bash run_qwen35_configurable.sh  # 单 GPU
NGPU=4  bash run_qwen35_configurable.sh  # 4 GPU
NGPU=8  bash run_qwen35_configurable.sh  # 8 GPU
NGPU=16 bash run_qwen35_configurable.sh  # 16 GPU
```

### Q2: 如何调整并行策略？
```bash
# 纯数据并行（最简单）
NGPU=8 bash run_qwen35_configurable.sh

# 数据并行 + 张量并行
NGPU=8 DP_SHARD=4 TP=2 bash run_qwen35_configurable.sh

# 数据并行 + 张量并行 + 流水线并行
NGPU=8 DP_SHARD=2 TP=2 PP=2 bash run_qwen35_configurable.sh
```

### Q3: 如何实时查看精度、显存、性能？

**方法 1 - 终端自动输出**（训练时自动显示）
```
Step 1/10 | Loss: 10.52 | Tokens/sec: 1234 | GPU Mem: 45.2 GB
```

**方法 2 - 监控脚本**（专门工具）
```bash
bash monitor_training.sh
# 选项 2: 只看 Loss（精度）
# 选项 3: 只看 Tokens/sec（性能）
# 选项 4: 只看 GPU Mem（显存）
```

**方法 3 - TensorBoard**（可视化）
```bash
tensorboard --logdir ./outputs/
# 浏览器: http://localhost:6006
```

---

## 📦 文件清单

### ⭐ 必读文档（按顺序）

1. **[开始使用_README.md](开始使用_README.md)** 👈 **从这里开始！**
   - 完整验证结果
   - 立即开始的三种方式
   - 核心问题快速解答

2. **[README_可配置训练方案.md](README_可配置训练方案.md)**
   - 总览文档
   - 使用场景示例
   - 监控要点总结

3. **[可配置训练脚本使用指南.md](可配置训练脚本使用指南.md)**
   - 详细参数说明
   - 完整使用场景
   - 故障排查指南

### 🛠️ 核心工具

4. **[run_qwen35_configurable.sh](run_qwen35_configurable.sh)** ⭐⭐⭐
   - **最重要的脚本**
   - 支持所有 GPU 配置和并行策略
   - 自动输出精度、显存、性能

5. **[examples_qwen35_training.sh](examples_qwen35_training.sh)** ⭐⭐
   - 12 种预设配置
   - 菜单式选择，新手友好

6. **[monitor_training.sh](monitor_training.sh)** ⭐⭐
   - 7 种监控模式
   - 实时查看 Loss、显存、性能

### 📖 补充文档

7. **[快速开始.md](快速开始.md)**
   - 环境准备
   - 三种运行方式
   - 常见问题

8. **[QWEN35_运行指南.md](QWEN35_运行指南.md)**
   - 动态图模式详解
   - 可用配置列表
   - 参数说明

9. **[QWEN35_环境准备指南.md](QWEN35_环境准备指南.md)**
   - 环境安装步骤
   - 故障排查详解
   - 快速诊断命令

### 🧪 测试工具

10. **[test_toolchain.sh](test_toolchain.sh)**
    - 验证所有脚本和文档
    - 14 项检查

11. **[check_qwen35_ready.sh](check_qwen35_ready.sh)**
    - 检查运行环境
    - Python、PyTorch、CUDA、文件

12. **[setup_and_run_qwen35.sh](setup_and_run_qwen35.sh)**
    - 一键安装和运行
    - 自动检测和修复

---

## 🚀 快速开始（三选一）

### 方式 1: 示例选择器（推荐新手）
```bash
bash examples_qwen35_training.sh
```
会列出 12 种配置，选择一个即可。

### 方式 2: 可配置脚本（推荐熟练用户）
```bash
# 快速测试（1 步）
STEPS=1 bash run_qwen35_configurable.sh

# 标准训练（10 步）
bash run_qwen35_configurable.sh

# 8 GPU 混合并行
NGPU=8 DP_SHARD=4 TP=2 bash run_qwen35_configurable.sh
```

### 方式 3: 完全自定义
```bash
NGPU=8 \
STEPS=1000 \
BATCH_SIZE=2 \
SEQ_LEN=2048 \
DP_SHARD=4 \
TP=2 \
COMPILE=true \
bash run_qwen35_configurable.sh
```

---

## 📊 实时监控指南

### 训练时（自动输出）
训练启动后，终端会显示：
```
[时间] Step X/Y | Loss: X.XXXX | Tokens/sec: XXXX | GPU Mem: XX.X GB
```

### 使用监控脚本（在另一个终端）
```bash
bash monitor_training.sh
```
选择：
- **2** → 只看 Loss（精度变化）
- **3** → 只看 Tokens/sec（训练速度）
- **4** → 只看 GPU Memory（显存使用）
- **5** → 只看 Grad Norm（梯度稳定性）
- **7** → 启动 TensorBoard

### TensorBoard 可视化
```bash
# 启动
tensorboard --logdir ./outputs/

# 浏览器打开
http://localhost:6006
```

---

## ⚙️ 参数速查表

### 基本参数
| 参数 | 默认 | 说明 | 示例 |
|------|------|------|------|
| `NGPU` | 1 | GPU 数量 | `NGPU=8` |
| `STEPS` | 10 | 训练步数 | `STEPS=1000` |
| `BATCH_SIZE` | 1 | Batch size | `BATCH_SIZE=2` |
| `SEQ_LEN` | 512 | 序列长度 | `SEQ_LEN=2048` |
| `COMPILE` | false | 编译模式 | `COMPILE=true` |

### 并行策略
| 参数 | 默认 | 说明 | 示例 |
|------|------|------|------|
| `DP_SHARD` | -1 | FSDP 分片 | `DP_SHARD=4` |
| `TP` | 1 | 张量并行 | `TP=2` |
| `PP` | 1 | 流水线并行 | `PP=2` |
| `EP` | 1 | 专家并行 | `EP=4` |
| `CP` | 1 | 上下文并行 | `CP=2` |

### 监控参数
| 参数 | 默认 | 说明 | 示例 |
|------|------|------|------|
| `METRICS_LOG_FREQ` | 1 | 输出频率 | `METRICS_LOG_FREQ=10` |
| `TENSORBOARD` | true | TensorBoard | `TENSORBOARD=true` |
| `ENABLE_PROFILING` | false | 性能分析 | `ENABLE_PROFILING=true` |

**完整参数**：见 [可配置训练脚本使用指南.md](可配置训练脚本使用指南.md)

---

## 💡 典型使用场景

### 场景 1: 环境验证
```bash
# 1. 测试工具链
bash test_toolchain.sh

# 2. 快速测试（1 步）
STEPS=1 bash run_qwen35_configurable.sh
```

### 场景 2: 单机多 GPU
```bash
# 8 GPU 数据并行
NGPU=8 bash run_qwen35_configurable.sh

# 8 GPU 混合并行
NGPU=8 DP_SHARD=4 TP=2 bash run_qwen35_configurable.sh
```

### 场景 3: 长时间训练
```bash
# 训练 10000 步，每 100 步保存
NGPU=8 STEPS=10000 CHECKPOINT_INTERVAL=100 \
bash run_qwen35_configurable.sh

# 监控（另一个终端）
bash monitor_training.sh
```

### 场景 4: 性能优化
```bash
# 启用编译 + 增大 batch
NGPU=8 COMPILE=true BATCH_SIZE=4 \
bash run_qwen35_configurable.sh
```

---

## 🔍 故障排查

### 步骤 1: 验证工具链
```bash
bash test_toolchain.sh
```

### 步骤 2: 检查环境
```bash
bash check_qwen35_ready.sh
```

### 步骤 3: 查看文档
- 环境问题 → [QWEN35_环境准备指南.md](QWEN35_环境准备指南.md)
- 使用问题 → [可配置训练脚本使用指南.md](可配置训练脚本使用指南.md)
- 配置问题 → [README_可配置训练方案.md](README_可配置训练方案.md)

### 步骤 4: 配置验证
```bash
# 使用 fake_backend 模式验证配置（不实际运行）
NGPU=8 COMM_MODE="fake_backend" DP_SHARD=4 TP=2 \
bash run_qwen35_configurable.sh
```

---

## 📚 推荐学习路径

### 第 1 天：基础
1. 阅读 [开始使用_README.md](开始使用_README.md)
2. 运行 `bash test_toolchain.sh`
3. 运行 `STEPS=1 bash run_qwen35_configurable.sh`
4. 运行 `bash monitor_training.sh` 查看监控

### 第 2 天：多 GPU
1. 阅读 [README_可配置训练方案.md](README_可配置训练方案.md)
2. 运行 `bash examples_qwen35_training.sh`
3. 尝试不同的 GPU 配置

### 第 3 天：并行策略
1. 阅读 [可配置训练脚本使用指南.md](可配置训练脚本使用指南.md)
2. 理解并行度计算规则
3. 尝试混合并行配置

### 第 4 天：优化
1. 启用编译模式
2. 调整 batch size 和序列长度
3. 使用性能分析工具

---

## 🎁 核心优势

✅ **灵活性**：所有参数都可配置  
✅ **易用性**：提供示例和监控工具  
✅ **可观测性**：实时输出 + TensorBoard  
✅ **调试友好**：支持配置验证和模拟  
✅ **文档完善**：从新手到进阶全覆盖

---

## 🎯 核心对比

### 原始 run_train.sh vs 新的可配置方案

| 特性 | run_train.sh | run_qwen35_configurable.sh |
|------|--------------|----------------------------|
| GPU 配置 | 仅 NGPU | NGPU + 所有并行参数 |
| 模型支持 | 通用 | 专为 Qwen3.5 优化 |
| 监控输出 | 基础 | 精度+显存+性能全覆盖 |
| 配置验证 | ✅ | ✅ + 配置打印 |
| 文档支持 | - | 8 份完整文档 |
| 辅助工具 | - | 监控脚本 + 示例选择器 |

---

## 📞 获取帮助

**首次使用？**  
→ 阅读 [开始使用_README.md](开始使用_README.md)

**遇到环境问题？**  
→ 运行 `bash check_qwen35_ready.sh`  
→ 阅读 [QWEN35_环境准备指南.md](QWEN35_环境准备指南.md)

**不知道如何配置？**  
→ 运行 `bash examples_qwen35_training.sh`  
→ 阅读 [可配置训练脚本使用指南.md](可配置训练脚本使用指南.md)

**想深入学习？**  
→ 阅读 [README_可配置训练方案.md](README_可配置训练方案.md)

---

## ✨ 立即开始

```bash
# 1. 验证环境
bash test_toolchain.sh

# 2. 快速测试
STEPS=1 bash run_qwen35_configurable.sh

# 3. 查看监控
bash monitor_training.sh

# 4. 开始训练
bash run_qwen35_configurable.sh
```

---

**🎉 祝你训练顺利！所有工具和文档都已就绪！**
