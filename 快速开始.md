# Qwen3.5 快速开始

## 🎯 最简单的方法：一键运行

```bash
bash setup_and_run_qwen35.sh
```

这个脚本会：
1. ✅ 自动检测 Python 环境
2. ✅ 检查 PyTorch 安装状态
3. ✅ 提示安装缺失的依赖
4. ✅ 验证必需文件（tokenizer、数据集）
5. ✅ 配置 PYTHONPATH
6. ✅ 运行 Qwen3.5 训练

## 📋 准备工作清单

### 必需项（运行前必须完成）

- [ ] **Python 3.10+** 已安装
  ```bash
  python --version  # 应显示 3.10 或更高
  ```

- [ ] **PyTorch** 已安装
  ```bash
  python -c "import torch; print(torch.__version__)"
  ```
  
  如果未安装，运行：
  ```bash
  # GPU 版本（推荐）
  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
  
  # 或 CPU 版本
  pip install torch torchvision torchaudio
  ```

- [ ] **项目依赖** 已安装
  ```bash
  pip install -r .ci/docker/requirements.txt
  ```

### 可选项（已自动准备）

- [x] **Tokenizer** - 已存在于 `tests/assets/tokenizer/`
- [x] **测试数据集** - 已存在于 `tests/assets/cc12m_test/`
- [x] **运行脚本** - 已创建

## 🚀 三种运行方式

### 方式 1: 一键运行（最简单）⭐

```bash
bash setup_and_run_qwen35.sh
```

脚本会引导你选择：
- 快速测试（1 步）
- 标准训练（10 步）
- 自定义训练

### 方式 2: 使用专用脚本

```bash
# 单 GPU
bash run_qwen35_eager.sh

# 多 GPU
NUM_GPUS=2 bash run_qwen35_eager.sh
```

### 方式 3: 直接命令

```bash
# 快速测试（1 步）
python -m torchtitan.train \
  --module torchtitan.models.qwen3_5.config_registry \
  --config qwen35_debugmodel \
  --training.compile.enable False \
  --training.steps 1

# 完整训练（10 步）
python -m torchtitan.train \
  --module torchtitan.models.qwen3_5.config_registry \
  --config qwen35_debugmodel \
  --training.compile.enable False
```

## 📊 验证环境

运行环境检查：
```bash
bash check_qwen35_ready.sh
```

应该看到：
```
✓ Python 已安装
✓ PyTorch 已安装
✓ CUDA 可用
✓ torchtitan 模块可导入
✓ qwen3.5 配置可加载
✓ Tokenizer 文件存在
✓ 测试数据集存在
```

## ⚠️ 常见问题

### 问题 1: Python 环境错误

**症状**: `python: command not found` 或 exit code 49

**解决方案**:
```bash
# 尝试使用 python3
python3 --version

# 或安装 Conda
# 下载 Miniconda: https://docs.conda.io/en/latest/miniconda.html
conda create -n torchtitan python=3.10 -y
conda activate torchtitan
```

### 问题 2: PyTorch 未安装

**症状**: `ModuleNotFoundError: No module named 'torch'`

**解决方案**:
```bash
# GPU 版本（访问 pytorch.org 获取最新命令）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# CPU 版本
pip install torch torchvision torchaudio
```

### 问题 3: CUDA 不可用

**症状**: `CUDA available: False`

**解决方案**:
- 确认 GPU 驱动已安装
- 安装正确的 CUDA 版本的 PyTorch
- 如果没有 GPU，可以使用 CPU 训练（较慢）

### 问题 4: 模块导入失败

**症状**: `ModuleNotFoundError: No module named 'torchtitan'`

**解决方案**:
```bash
# 设置 PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:$(pwd)"

# 安装依赖
pip install -r .ci/docker/requirements.txt
```

## 📚 完整文档

- **[QWEN35_运行指南.md](QWEN35_运行指南.md)** - 详细使用说明
- **[QWEN35_环境准备指南.md](QWEN35_环境准备指南.md)** - 环境配置详解

## 🎯 成功标志

训练成功运行时，你会看到类似输出：

```
==========================================
开始训练 Qwen3.5
==========================================

[2026-07-16 10:00:00] INFO: Model initialized
[2026-07-16 10:00:01] INFO: Step 1/10 | Loss: 10.5234 | Tokens/sec: 1234
[2026-07-16 10:00:02] INFO: Step 2/10 | Loss: 10.2156 | Tokens/sec: 1245
...
[2026-07-16 10:00:10] INFO: Training completed

✓ 训练成功完成！
```

## 🔍 如果还有问题

1. 运行完整检查：`bash check_qwen35_ready.sh`
2. 查看错误日志：`cat outputs/train.log`（如果存在）
3. 参考环境准备指南：`cat QWEN35_环境准备指南.md`

---

**提示**: 首次运行建议选择"快速测试（1 步）"来验证环境配置正确。
