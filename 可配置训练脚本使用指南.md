# Qwen3.5 可配置训练脚本使用指南

## 📋 脚本文件

**[run_qwen35_configurable.sh](run_qwen35_configurable.sh)** - 功能强大的可配置训练脚本

## 🎯 核心功能

✅ **灵活调整 GPU 数量**：支持 1-N 个 GPU  
✅ **可配置并行策略**：DP、TP、PP、EP、CP 全支持  
✅ **实时监控输出**：精度、显存、性能信息  
✅ **调试模式**：单 GPU 模拟多 GPU 训练  
✅ **自动化日志**：TensorBoard、检查点、性能分析

---

## 🚀 快速使用

### 1. 单 GPU 训练（最简单）
```bash
bash run_qwen35_configurable.sh
```

### 2. 多 GPU 数据并行
```bash
# 4 GPU，自动使用 FSDP
NGPU=4 bash run_qwen35_configurable.sh

# 8 GPU，自动使用 FSDP
NGPU=8 bash run_qwen35_configurable.sh
```

### 3. 混合并行（推荐大模型）
```bash
# 8 GPU：DP=2, TP=2, PP=2
NGPU=8 DP_SHARD=2 TP=2 PP=2 bash run_qwen35_configurable.sh

# 16 GPU：DP=4, TP=2, PP=2
NGPU=16 DP_SHARD=4 TP=2 PP=2 bash run_qwen35_configurable.sh
```

### 4. 长训练任务
```bash
# 训练 1000 步，每 50 步保存检查点
NGPU=8 STEPS=1000 CHECKPOINT_INTERVAL=50 bash run_qwen35_configurable.sh
```

### 5. 启用编译模式（更快）
```bash
# 使用 torch.compile 加速
NGPU=4 COMPILE=true bash run_qwen35_configurable.sh
```

---

## 📊 如何实时查看精度、显存、性能信息

### 方法 1: 终端实时输出（最直接）

训练过程中，终端会**自动显示**：

```
[2026-07-16 10:00:01] Step 1/10 | Loss: 10.5234 | Tokens/sec: 1234.5 | GPU Mem: 45.2 GB
[2026-07-16 10:00:02] Step 2/10 | Loss: 10.2156 | Tokens/sec: 1245.8 | GPU Mem: 45.3 GB
[2026-07-16 10:00:03] Step 3/10 | Loss: 9.9871  | Tokens/sec: 1238.2 | GPU Mem: 45.3 GB
```

**输出信息包括**：
- ✅ **Loss（精度）**：每步的损失值，越低越好
- ✅ **Tokens/sec（性能）**：每秒处理的 token 数，越高越好
- ✅ **GPU Mem（显存）**：当前 GPU 显存使用量
- ✅ **Grad Norm**：梯度范数（用于判断训练稳定性）

**调整输出频率**：
```bash
# 每步都输出（默认）
METRICS_LOG_FREQ=1 bash run_qwen35_configurable.sh

# 每 10 步输出一次
METRICS_LOG_FREQ=10 bash run_qwen35_configurable.sh
```

### 方法 2: TensorBoard 实时可视化（推荐）

**启动训练**：
```bash
NGPU=4 TENSORBOARD=true bash run_qwen35_configurable.sh
```

**在另一个终端启动 TensorBoard**：
```bash
tensorboard --logdir ./outputs/qwen35_*/
```

**在浏览器打开**：`http://localhost:6006`

**TensorBoard 显示内容**：
- 📈 **Loss 曲线**：训练损失随时间变化
- 📊 **学习率曲线**：LR scheduler 变化
- 💾 **显存使用**：每个 GPU 的显存占用
- ⚡ **吞吐量**：Tokens/sec、TFLOPS、MFU
- 🔢 **梯度统计**：梯度范数、梯度分布

### 方法 3: 日志文件实时跟踪

**在另一个终端监控日志**：
```bash
# 实时查看最新日志
tail -f ./outputs/qwen35_*/train.log

# 只看 Loss
tail -f ./outputs/qwen35_*/train.log | grep "Loss:"

# 只看显存
tail -f ./outputs/qwen35_*/train.log | grep "GPU Mem"
```

### 方法 4: 启用性能分析（详细分析）

```bash
# 启用 PyTorch Profiler
ENABLE_PROFILING=true PROFILE_FREQ=5 bash run_qwen35_configurable.sh
```

**查看性能分析结果**：
```bash
# Chrome 中打开 trace 文件
chrome://tracing
# 加载 outputs/qwen35_*/profile_*.json
```

### 方法 5: 显存快照（调试 OOM）

```bash
# 启用显存快照
MEMORY_SNAPSHOT=true bash run_qwen35_configurable.sh
```

**分析显存使用**：
```bash
# 显存快照保存在
ls ./outputs/qwen35_*/memory_snapshots/
```

---

## ⚙️ 完整参数配置

### 基本参数

| 参数 | 默认值 | 说明 | 示例 |
|------|--------|------|------|
| `NGPU` | 1 | GPU 数量 | `NGPU=8` |
| `STEPS` | 10 | 训练步数 | `STEPS=1000` |
| `BATCH_SIZE` | 1 | 本地 batch size | `BATCH_SIZE=2` |
| `SEQ_LEN` | 512 | 序列长度 | `SEQ_LEN=2048` |
| `COMPILE` | false | 是否编译 | `COMPILE=true` |
| `CONFIG` | qwen35_debugmodel | 模型配置 | `CONFIG=qwen35_2b` |

### 并行策略参数

| 参数 | 默认值 | 说明 | 推荐配置 |
|------|--------|------|----------|
| `DP_SHARD` | -1 | FSDP 分片度 | `-1`（自动）或 `2/4/8` |
| `DP_REPLICATE` | 1 | DDP 复制度 | HSDP 时设为 `>1` |
| `TP` | 1 | 张量并行度 | `2/4/8` |
| `PP` | 1 | 流水线并行度 | `2/4/8` |
| `EP` | 1 | 专家并行度（MoE） | `2/4/8` |
| `CP` | 1 | 上下文并行度 | `2/4` |

### 监控参数

| 参数 | 默认值 | 说明 | 示例 |
|------|--------|------|------|
| `METRICS_LOG_FREQ` | 1 | 指标输出频率 | `METRICS_LOG_FREQ=10` |
| `TENSORBOARD` | true | 启用 TensorBoard | `TENSORBOARD=true` |
| `ENABLE_PROFILING` | false | 性能分析 | `ENABLE_PROFILING=true` |
| `MEMORY_SNAPSHOT` | false | 显存快照 | `MEMORY_SNAPSHOT=true` |

### 检查点参数

| 参数 | 默认值 | 说明 | 示例 |
|------|--------|------|------|
| `CHECKPOINT_ENABLE` | true | 启用检查点 | `CHECKPOINT_ENABLE=true` |
| `CHECKPOINT_INTERVAL` | 100 | 保存间隔 | `CHECKPOINT_INTERVAL=50` |
| `CHECKPOINT_FOLDER` | 自动生成 | 保存路径 | `CHECKPOINT_FOLDER=./ckpt` |

---

## 📖 使用场景示例

### 场景 1: 快速验证环境（1 步测试）
```bash
STEPS=1 bash run_qwen35_configurable.sh
```

### 场景 2: 单机 8 GPU 训练
```bash
# 纯数据并行（最简单）
NGPU=8 STEPS=1000 bash run_qwen35_configurable.sh

# 数据并行 + 张量并行（更大模型）
NGPU=8 DP_SHARD=4 TP=2 STEPS=1000 bash run_qwen35_configurable.sh
```

### 场景 3: 大规模训练（16+ GPU）
```bash
# 16 GPU: DP=4, TP=2, PP=2
NGPU=16 DP_SHARD=4 TP=2 PP=2 STEPS=10000 \
CHECKPOINT_INTERVAL=100 \
bash run_qwen35_configurable.sh

# 32 GPU: DP=8, TP=2, PP=2
NGPU=32 DP_SHARD=8 TP=2 PP=2 STEPS=10000 \
bash run_qwen35_configurable.sh
```

### 场景 4: MoE 模型训练
```bash
# 使用专家并行
CONFIG=qwen35_35b_a3b \
NGPU=16 DP_SHARD=2 TP=2 EP=4 \
bash run_qwen35_configurable.sh
```

### 场景 5: 长序列训练
```bash
# 使用上下文并行处理长序列
NGPU=8 SEQ_LEN=8192 CP=2 DP_SHARD=4 \
bash run_qwen35_configurable.sh
```

### 场景 6: 调试模式（单 GPU 模拟 8 GPU）
```bash
# 验证 8 GPU 配置，但只用 1 个 GPU
NGPU=8 COMM_MODE="local_tensor" \
DP_SHARD=4 TP=2 \
bash run_qwen35_configurable.sh
```

### 场景 7: 性能优化测试
```bash
# 编译模式 + 性能分析
NGPU=8 COMPILE=true \
ENABLE_PROFILING=true \
PROFILE_FREQ=5 \
bash run_qwen35_configurable.sh
```

### 场景 8: 显存优化调试
```bash
# 减小 batch 和序列长度，启用显存快照
BATCH_SIZE=1 SEQ_LEN=256 \
MEMORY_SNAPSHOT=true \
bash run_qwen35_configurable.sh
```

---

## 🔍 监控要点解读

### 1. Loss（训练精度）

**正常情况**：
- Loss 应该**逐步下降**
- 下降速度：开始快，后期慢
- 波动：小幅波动正常

**异常情况**：
- ⚠️ **Loss 不降**：学习率太小或数据有问题
- ⚠️ **Loss 爆炸**：学习率太大或梯度裁剪失效
- ⚠️ **Loss 为 NaN**：数值不稳定，检查混合精度设置

### 2. GPU Memory（显存使用）

**查看位置**：
- 终端输出：`GPU Mem: XX.X GB`
- TensorBoard：Memory 标签页

**优化建议**：
- 如果 **OOM（显存溢出）**：
  - 减小 `BATCH_SIZE`
  - 减小 `SEQ_LEN`
  - 增加 `DP_SHARD`（更多分片）
  - 启用 `--training.enable_cpu_offload`

### 3. Throughput（训练性能）

**关键指标**：
- **Tokens/sec**：每秒处理的 token 数，越高越好
- **TFLOPS**：浮点运算吞吐量
- **MFU**：模型 FLOPS 利用率（越接近 100% 越好）

**优化建议**：
- 提升性能：
  - 启用 `COMPILE=true`（torch.compile）
  - 增大 `BATCH_SIZE`（充分利用 GPU）
  - 使用 `ENABLE_ASYNC_TP=true`（异步 TP）

### 4. Grad Norm（梯度稳定性）

**正常范围**：通常在 0.1 - 10 之间

**异常情况**：
- ⚠️ **Grad Norm 突然增大**：可能梯度爆炸
- ⚠️ **Grad Norm 接近 0**：可能梯度消失

---

## 💡 并行策略选择指南

### GPU 数量与并行策略对应

| GPU 数量 | 推荐策略 | 命令示例 |
|---------|---------|----------|
| 1 个 | 单 GPU | `bash run_qwen35_configurable.sh` |
| 2-4 个 | 纯数据并行 | `NGPU=4 bash run_qwen35_configurable.sh` |
| 8 个 | DP + TP | `NGPU=8 DP_SHARD=4 TP=2` |
| 16 个 | DP + TP + PP | `NGPU=16 DP_SHARD=4 TP=2 PP=2` |
| 32+ 个 | 全维度并行 | `NGPU=32 DP_SHARD=8 TP=2 PP=2` |

### 并行度计算规则

**必须满足**：`NGPU = DP_REPLICATE × DP_SHARD × TP × PP × EP × CP`

**示例**：
- 8 GPU，DP=4, TP=2：`4 × 2 = 8` ✅
- 16 GPU，DP=4, TP=2, PP=2：`4 × 2 × 2 = 16` ✅
- 8 GPU，DP=2, TP=2, PP=4：`2 × 2 × 4 = 16` ❌（需要 16 个 GPU）

---

## 🛠️ 故障排查

### 问题 1: 脚本没有执行权限
```bash
chmod +x run_qwen35_configurable.sh
```

### 问题 2: 参数配置错误
```bash
# 使用调试模式验证配置
NGPU=8 COMM_MODE="fake_backend" bash run_qwen35_configurable.sh
```

### 问题 3: 看不到实时输出
```bash
# 确保 LOG_RANK 设置正确
LOG_RANK=0 NGPU=4 bash run_qwen35_configurable.sh

# 或查看日志文件
tail -f ./outputs/qwen35_*/train.log
```

### 问题 4: TensorBoard 不显示数据
```bash
# 确保 TensorBoard 启用
TENSORBOARD=true bash run_qwen35_configurable.sh

# 检查输出目录
tensorboard --logdir ./outputs/
```

---

## 📚 相关文档

- **[快速开始.md](快速开始.md)** - 环境准备和快速上手
- **[QWEN35_运行指南.md](QWEN35_运行指南.md)** - 详细使用文档
- **[QWEN35_环境准备指南.md](QWEN35_环境准备指南.md)** - 环境配置详解

---

## ✨ 推荐工作流

### 第一次使用
1. **环境测试**：`STEPS=1 bash run_qwen35_configurable.sh`
2. **短期训练**：`STEPS=10 bash run_qwen35_configurable.sh`
3. **监控验证**：启动 TensorBoard 查看指标

### 日常训练
1. **配置参数**：根据 GPU 数量设置并行策略
2. **启动训练**：使用对应的命令
3. **实时监控**：在另一个终端 `tail -f` 日志或查看 TensorBoard
4. **定期检查**：查看 Loss、显存、性能是否正常

### 性能优化
1. **基准测试**：记录初始性能指标
2. **启用编译**：`COMPILE=true`
3. **调整 batch size**：增大到显存允许的最大值
4. **性能分析**：`ENABLE_PROFILING=true` 找瓶颈

---

**提示**：所有参数都可以组合使用，根据你的需求灵活配置！
